<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.doantest1">

    <!-- Thêm quyền in -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Doantest1"
        tools:targetApi="34">

        <!-- Login Activity (launcher) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity (sau khi login) -->
        <activity android:name=".MainActivity" />

        <!-- Manage Teacher -->
        <activity android:name=".ManageTeachersActivity"/>

        <!-- Manage Student Activity -->
        <activity android:name=".StudentManagementActivity" />

        <!-- Face Check Activity -->
        <activity android:name=".FaceCheckActivity" />

        <!-- Manage Exam Periods Activity -->
        <activity android:name=".ManageExamPeriodsActivity" />
        <!-- Manage Exam Date Activity -->
        <activity android:name=".ManageExamDatesActivity" />
        <!-- Manage Exam Sessions Activity -->
        <activity android:name=".ManageExamSessionsActivity" />
        <!-- Manage Exam Rooms Activity -->
        <activity android:name=".ManageExamRoomsActivity" />

        <!-- Room Attendance Activity -->
        <activity android:name=".RoomAttendanceActivity" />

        <!-- Teacher Exam Room Activity (Giao diện giáo viên) -->
        <activity android:name=".TeacherExamRoomsActivity" />


        <!-- Add Student Activity -->
        <activity android:name=".AddStudentActivity" />
        <!-- Show Student Activity -->
        <activity android:name=".ShowStudentActivity" />

        <!-- Show Attendance Results Activity -->
        <activity android:name=".AttendanceResultActivity" />


    </application>

</manifest>
